# vmojifast demo

A Vue 3 proof of concept for the local `vmojifast` source.

```bash
npm run demo
```

The demo aliases `vmojifast` to `../src/index.ts`, so it always exercises the current library source rather than a published npm build.
